<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="stdmet"
        android:summary="Switch between Standard and Metric"
        android:title="Use US Standard Units" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
<!--    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/Artist"
        android:inputType="textCapWords"
        android:key="artist"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/Artist" />

    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/Location"
        android:inputType="textCapWords"
        android:key="location"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/Location" />-->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="33"
        android:entries="@array/pref_coe"
        android:entryValues="@array/pref_coe_list_values"
        android:key="pref_coe"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_which_coe" />


</PreferenceScreen>
